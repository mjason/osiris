[project]
name = "osiris-stdlib"
version = "0.3.2"
description = "Source-distributed standard library for Osiris"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[build-system]
requires = ["osiris-lang>=0.3,<0.4"]
build-backend = "osiris_build"
